pipeline {
    
    agent any

    stages {
        stage ('Deploy') {
            steps {
                echo "Hello World"
                sh '''
                    pwd
                    ls -ltrh
                '''
            }
        }
    }
}