pipeline {
    
    agent any

    stages {
        stage ('Deploy') {
            steps {
                echo "My Timesheet Reminder Deployment"
                sh '''
                    pwd
                    ls -ltrh
                '''
            }
        }
    }
}